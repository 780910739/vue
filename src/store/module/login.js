import * as types from '../types';
import router from './../../router';
// initial state
const state = {
}

// getters
const getters = {}
// actions
const actions = {
}
// mutations
const mutations = {
}

const initState = JSON.parse(JSON.stringify(state));

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
